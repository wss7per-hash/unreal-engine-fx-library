# UE5 特效库管理软件 · 产品设计与技术白皮书（纯客户端架构）

> 形态：**独立桌面客户端（Python / PySide6）+ UE 无头桥（Headless Bridge）**
> 目标用户：独立开发者 / 小团队 / 中大型工作室（免费个人版打底，团队功能商业化）
> MVP 核心卖点：跨项目资产迁移（特效 + 依赖一键搬运，不丢引用）
> **预览定义（重要）：仅静态缩略图** —— 显示该特效的一张代表性截图即可，**不做实时播放 / 视频循环**。这大幅降低预览模块的工程量与风险。
> ⚠️ **架构调整说明（2026-07-15）**：经确认，**取消 UE 编辑器插件形态**，只做独立客户端。引擎敏感操作（读资产 / 出缩略图 / 导入导出）由客户端**后台拉起 UE 无头模式跑 Python 脚本**完成。原本插件里的逻辑全部下沉为「UE 桥脚本」，客户端通过本地进程调用它们。

---

## 0. 为什么能去掉插件（关键认知）

独立客户端**自己无法读取 `.uasset`、也无法渲染 Niagara/Cascade 缩略图** —— 这些操作必须靠 UE 引擎本体（`AssetRegistry`、`AssetTools`、缩略图系统都只在编辑器进程里可用）。

但这**不等于必须有一个"嵌在编辑器里的插件"**。我们只需要「一个能运行 UE Python 脚本的引擎实例」即可，而这个实例可以由客户端**从外部拉起、用完即退（或常驻）**：

- 客户端 = **GUI + 库索引（SQLite）+ `.fxpack` 管理**，完全脱离引擎，随时可用。
- UE 桥 = 客户端随附的一包 Python 脚本（`fx_list / fx_thumbnail / fx_export / fx_import` 等），在需要时由客户端通过命令行 `UnrealEditor.exe <Project> -ExecutePythonScript=<bridge>/fx_runner.py` 拉起引擎**无头（不弹编辑器窗口）执行**，干完引擎的活、把结果（缩略图 JPG、manifest、导出包）落到磁盘后退出。

> 一句话：**引擎仍是"唯一真相源"，但我们不再把 UI 焊死在编辑器里——客户端在外部编排引擎。** 这比插件方案更干净：库管理在编辑器之外做，不污染项目、不拖垮编辑器、跨项目无需每个工程都装插件。

---

## 1. 产品定位与一句话价值

**"特效资产的 Quixel Bridge + 资产管理器"** —— 让美术和开发者像管理图片素材一样管理 Niagara / Cascade 粒子特效：能看、能搜、能收藏、能一键在任意 UE5 项目间搬运，且自动带上所有依赖、不丢引用、不变粉。

### 为什么值得做

| 现状痛点 | 本软件的解法 |
|---|---|
| UE5 没有好用的「特效资产库」，只能靠 Content Browser 文件夹硬翻 | 统一元数据 + 标签 + 缩略图 + 全文检索 |
| 把特效从 A 项目搬到 B 项目要手动 Migrate，经常丢材质/曲线/贴图，打开变粉 | 依赖图递归提取 + 智能重映射，导出自包含包 |
| 粒子预览只能进引擎看，库外面完全盲选 | 客户端通过 UE 桥截取静态缩略图并缓存，离线浏览（不需播放） |
| 团队里同一个爆炸特效被复制了 20 份、版本各异 | 统一库 + 版本/收藏 + 复用统计 |
| 社区资产散落 Marketplace / 论坛 / 网盘，没有聚合 | 内置社区市场（用户互传、评分、标签） |

---

## 2. 目标用户与典型场景

### 2.1 用户分层

- **个人 / 独立开发者**：要免费、轻量、开箱即用。核心诉求是「快速找现成特效 + 不丢依赖地复用」。本机装 UE 即可用全部功能。
- **小团队（3-15 人）**：开始需要标签、收藏夹、团队共享库、基础权限。
- **中大型工作室 / 美术团队**：需要私有云库、版本管理、审批流、资产健康扫描、批量管线集成。

### 2.2 高频场景

1. **场景复用**：美术在 ProjectA 做了高质量火焰，ProjectB 在客户端里搜到 → 一键导入（客户端拉起 UE 桥把 `.fxpack` 解包重映射进当前项目），3 秒可用。
2. **外包 / 接包交付**：把做好的特效打包成 `.fxpack` 发给客户，对方导入零配置。
3. **资产体检**：扫描项目里所有粒子，列出「缺失依赖 / 重复资产 / 超 GPU 预算」清单（由 UE 桥在引擎内跑，客户端展示结果）。
4. **社区获取**：在社区市场按「风格 + 性能档位」筛选免费特效，一键拉进项目。
5. **版本回滚**：某个冲击波特效改坏了，从版本历史回退到上一版。

---

## 3. 系统架构（纯客户端 + UE 无头桥）

```
┌──────────────────────────────────────────────────────────────────┐
│               独立桌面客户端 (Standalone Desktop Client)             │
│  技术栈：Python 3 + PySide6（Qt）                                    │
│  • 库浏览 · 检索 · 标签 · 收藏 · 社区市场 · 版本查看 · 分享            │
│  • 展示每个特效的静态缩略图（thumb.jpg），不做播放                      │
│  • 本地 SQLite 索引 + 本地库目录（.fxpack 仓库）                      │
│  • 进程编排：按需拉起 UE 无头桥（见下）                               │
└───────────────▲───────────────────────────┬──────────────────────┘
                │ ① 写 request.json           │ 读 .fxpack / 缩略图
                │ ② 拉起 UE 无头进程           │ (离线浏览, 不依赖引擎)
                │ ③ 读 result.json + 产物      │
┌───────────────┴───────────────────────────▼──────────────────────┐
│              UE 无头桥 (Headless Bridge = 随附 Python 脚本包)         │
│  由客户端通过命令行启动：                                            │
│  UnrealEditor.exe <Project>.uproject                               │
│     -ExecutePythonScript=<bridge>/fx_runner.py                    │
│     -unattended -NoSplash -RenderOffScreen -ABSLOG=ue.log          │
│  • fx_runner.py   : 读 request.json → 分发到下列模块 → 写 result.json │
│  • fx_list.py     : 列资产（AssetRegistry，递归依赖）                │
│  • fx_thumbnail.py: 生成静态缩略图（Tier1/2/3）                       │
│  • fx_export.py   : 打包 .fxpack（递归依赖 + manifest + 缩略图）      │
│  • fx_import.py   : 解包 + 智能重映射 + 冲突处理 + 体检              │
│  • fx_health.py   : 资产健康扫描                                     │
└───────────────▲──────────────────────────────────────────────────┘
                │ 操作 UE5 资产 (AssetRegistry / AssetTools / EditorAssetLibrary)
                ▼
        UE5 引擎 (Niagara System / Cascade / 依赖资源)
```

### 3.1 客户端 ↔ UE 桥的通信契约（核心设计）

> 难点：UE 的 `-ExecutePythonScript` 模式对命令行参数解析不友好，且引擎进程生命周期要管。采用**「请求/结果文件 + 进程等待」**契约，规避 argv 脆弱性：

1. **客户端准备请求**：把要执行的命令和参数写成 `<tmp>/fx_request.json`：
   ```json
   { "command": "export",
     "params": { "project": "E:/.../MyProject.uproject",
                 "assets": ["/Game/FX/NS_Fire"], "out": "E:/FXLib/" } }
   ```
2. **客户端启动引擎（无头）**：
   ```
   UnrealEditor.exe "<Project>.uproject" \
     -ExecutePythonScript="<bridge>/fx_runner.py" \
     -unattended -NoSplash -RenderOffScreen -ABSLOG="<tmp>/ue.log"
   ```
   - `-RenderOffScreen`：无窗口但保留渲染能力（缩略图 Tier2 峰值帧需要）。
   - `-unattended`：不弹对话框、不阻塞、自动退出。
3. **桥脚本执行**：`fx_runner.py` 读 `fx_request.json` → 调用对应模块 → 把产物（缩略图 JPG / `.fxpack` / manifest）落盘 → 写 `<tmp>/fx_result.json`（`{ "ok": true, "data": {...} }` 或 `{ "ok": false, "error": "..." }`）。
4. **客户端收结果**：等待引擎进程退出（或监听 `fx_result.json` 出现）→ 读结果 → 刷新 UI。日志实时 tail `<tmp>/ue.log` 显示进度。

### 3.2 两种运行模式（MVP 先实现 oneshot，server 为增强）

- **oneshot（MVP 默认）**：每次操作拉起一次引擎、干完退出。实现简单、隔离好；代价是冷启动慢（首次 ~10–30s，取决于项目大小与 DDC）。
- **server（增强，推荐后期）**：桥脚本带 `--server` 参数时启动一个**常驻本地 HTTP 服务**（如 `localhost:17899`）并保持引擎存活；客户端走 HTTP 发请求、拿响应，**避免每次重拉引擎**。同一项目连续操作时体验接近原生。端口占用/引擎崩溃时客户端自动降级回 oneshot 并提示。

> 客户端须检测：本机是否装了 UE、UE 版本是否 ≥ 5.3、目标项目是否可加载（`.uproject` 存在且未损坏）。这些前置检查在客户端侧用文件探测 + 版本号正则即可，不必拉起引擎。

### 关键架构原则

- **引擎是唯一真相源，但 UI 在外部**：缩略图截取、依赖解析、导入导出全由 UE 桥在引擎内完成；客户端只消费结果（元数据 + 缩略图 + `.fxpack`）。
- **缩略图在引擎内截、客户端只显示**：粒子仍需引擎渲染才能得到画面，但只需截**一张静态图**（单帧），不涉及视频编码/循环播放，成本极低。
- **通过「本地库文件」解耦**：桥把每个特效导出为 `.fxpack`（自包含目录/zip），客户端读这个文件即可离线浏览，不依赖引擎常开。
- **无头 ≠ 不可见渲染**：Tier1 复用引擎缓存缩略图（无需渲染）；Tier2 峰值帧需 `-RenderOffScreen` 真正渲染一帧，已在对引擎的启动参数里预留。

---

## 4. 核心数据模型（`.fxpack` 与元数据）

### 4.1 `.fxpack` 是什么（注意：不是新的资产格式）

**`.fxpack` 本质是一个「带清单的 zip 容器」，里面装的 100% 是标准 `.uasset`**。引擎不认识 `.fxpack` 后缀，它只在「库管理 / 社区分享」层存在；解包进 UE 后，资产全部回归原生 `.uasset`，引擎零感知。

```
my_fire.fxpack  (== 一个 zip 包)
├── manifest.json          # 我们附加的元数据（标签/性能档/作者/许可），.uasset 装不下
├── assets/                # 全是标准 .uasset（特效本体 + 递归依赖）
│   ├── NS_Fire.uasset
│   ├── M_Fire.uasset
│   ├── T_Noise.uasset
│   └── ...（递归提取的材质/贴图/曲线/网格）
├── preview/
│   └── thumb.jpg          # 静态缩略图（唯一预览产物，无视频）
└── deps_graph.json        # 依赖关系图（用于导入时重映射）
```

#### 为什么不直接用裸 `.uasset` / 原生 Migrate？

> 关键点：**一个 Niagara 特效从来不是单个 .uasset**，它引用材质、贴图、曲线、网格、RenderTarget……只拷 `NS_Fire.uasset` 一个文件，导入别的项目必然变粉。

| 方式 | 打包全部依赖 | 单文件可分享 | 带元数据/缩略图 | 跨项目智能重映射 |
|---|---|---|---|---|
| 裸 `.uasset` | ❌ 仅单个文件 | ✅ | ❌ | ❌ |
| UE 原生 Migrate | ✅（按文件夹） | ❌ 需源项目开着、操作文件夹 | ❌ | 部分（靠路径） |
| **`.fxpack`（本软件）** | ✅ 递归全部依赖 | ✅ 一个文件，可邮件/上传/发市场 | ✅ manifest + jpg | ✅ deps_graph 智能重映射 |

**`.fxpack` 的三点价值**：
1. **自包含**：特效 + 全部依赖 `.uasset` 捆成一个文件，别人导入零配置、不变粉。
2. **可分发**：单文件可上传社区市场、邮件发送、按 hash 去重、做版本。
3. **带外信息**：`.uasset` 装不下标签/性能档/作者/缩略图，这些让独立客户端脱离引擎也能浏览检索。

> 一句话总结：**`.fxpack` 给 `.uasset` 穿上「可分享外衣」，引擎内部始终是原生 `.uasset`。** 实现上就是 `zip + manifest.json`，是轻量封装而非自定义二进制格式，不存在"造新格式"的兼容负担。

### 4.2 特效元数据（manifest.json 关键字段）

| 字段 | 说明 |
|---|---|
| `id` / `name` | 唯一 ID / 显示名 |
| `type` | `Niagara` / `Cascade` |
| `engineVersion` | 导出时 UE 版本（如 5.4） |
| `gpuCpu` | `GPU` / `CPU` / `Mixed` |
| `approxParticleCount` | 估算粒子数（性能档参考） |
| `modules` | 用到的 Niagara 模块列表（用于标签/检索） |
| `tags` | 风格标签：火焰 / 爆炸 / 魔法 / UI / 环境… |
| `author` / `license` | 作者 / 许可协议 |
| `dependencies` | 依赖资产列表（带原始路径，用于重映射） |
| `perfTier` | 性能档位：Low / Mid / High（美术选型参考） |

> 元数据来源：优先用 AssetRegistry 的自定义标签（`AssetRegistrySearchable`），没有的用 Python 反射读取 Niagara System 属性（模拟次数、GPU 标志等）。这些都在 UE 桥脚本里完成，客户端只存结果。

---

## 5. 核心功能模块

### 模块 A：资产浏览器（客户端 GUI）
- 客户端主窗口列出所选 UE 项目里的所有 Niagara System / Cascade Particle System（列表由 `fx_list.py` 经 UE 桥返回）。
- 网格视图 + 静态缩略图（无播放，无悬浮视频）。
- 右键菜单：导出 `.fxpack`、生成/刷新缩略图、加入收藏、查看依赖树。

### 模块 B：缩略图生成器（预览 = 静态图，非播放）
> 目标：为每个特效生成**一张代表性静态缩略图**，供客户端展示。**不做视频、不做实时播放。**
> **三档方案全部纳入范围**（用户明确要求），按下列优先级组合使用，且用户可随时手动覆盖。

- **Tier 1 · 复用引擎缓存缩略图（默认 / MVP）**：UE5 的 Content Browser 本就会为资产生成缩略图并缓存（`IAssetRegistry::GetThumbnailData` 可无头读取 PNG 字节）。桥脚本直接读缓存导出为 JPG/PNG，几乎零成本、无需自建渲染管线。**作为每个特效的默认缩略图。** 若缓存缺失，桥脚本触发一次引擎渲染（需 `-RenderOffScreen`）补齐。
- **Tier 2 · 截取代表性峰值帧（增强）**：在引擎内把特效放进临时预览场景，模拟推进到「视觉信息量最大」的时刻（爆炸展开峰值帧，固定 seek time 或粒子数峰值帧），用离屏 RenderTarget 截一帧 → 保存为缩略图。比缓存缩略图更能代表效果，**作为"更好看"的推荐缩略图**，用户可一键重新生成。
- **Tier 3 · 用户手动指定（覆盖 / 兜底）**：客户端允许用户手动截图或从本地选一张图作为该特效的缩略图。**优先级最高**——一旦用户指定，就覆盖 Tier1/2 的结果；同时也作为自动化方案都失败时的兜底。
- **组合逻辑**：导出 `.fxpack` 时带上的 `thumb.jpg` = 用户指定图（若有）＞ Tier2 峰值帧（若已生成）＞ Tier1 引擎缓存缩略图（必有）。
- 相比"播放视频"，静态缩略图**不需要 headless 视频渲染、不需要视频编码、不吃持续 GPU、体积极小**，是本项目难度显著下降的关键。

### 模块 C：依赖提取器 + 打包（MVP 重点，在 UE 桥内）
- 用 `AssetRegistry.GetDependencies(target, Recursive)` 递归拿到全部依赖。
- 过滤掉引擎内置资产（如 `/Engine/`、`/Plugin/` 下的基础材质），只打包项目级依赖。
- 生成 `deps_graph.json` 记录原始路径，供导入重映射。

### 模块 D：智能导入 + 引用重映射（MVP 重点，在 UE 桥内）
- 解包 `.fxpack` → 放入用户指定目录（默认 `/Game/ImportedFX/`）。
- **重映射**：扫描导入后的 `.uasset`，把内部引用从「原始项目路径」改到「当前项目实际路径」；对同名冲突资产提示「覆盖 / 重命名 / 跳过」。
- 导入后自动触发一次资产健康检查（是否有变粉/缺失引用），结果回传客户端展示。

### 模块 E：资产健康扫描（附加价值，易出彩，在 UE 桥内）
- 扫描项目全部粒子：缺失依赖、重复资产（按哈希）、超 GPU 预算、黑洞引用。
- 结果回传客户端，输出可点击的报告，直接跳到问题资产（提示用户在编辑器打开）。

### 模块 F：独立客户端 + 社区（增长引擎）
- 库浏览、标签筛选、收藏夹、合集分享、版本时间线。
- 每个特效展示静态缩略图（`thumb.jpg`），点开看详情/元数据；**客户端不渲染、不播放特效**，只做图片展示，实现极简。
- 社区市场：用户上传 `.fxpack`，别人按标签/性能检索、评分、下载。
- 本地 SQLite 存索引，缩略图离线看；在线时同步社区。

---

## 6. 让更多用户喜欢的策略（增长设计）

1. **免费个人版 + 开箱 Starter Pack**：内置 30-50 个常用特效（火/烟/爆炸/魔法/UI），下载即玩，零门槛建立口碑。
2. **社区资产市场**：用户互传特效、评分、打标签，形成网络效应（类似 Quixel / Sketchfab）。
3. **性能档位标注**：美术按「Low/Mid/High GPU 预算」选型，移动端开发者极度受用。
4. **与 UE Marketplace / ArtStation 联动**：支持导入商城购买的特效进统一库。
5. **「复用统计」**：告诉团队「这个特效被 8 个项目用过」——推动团队标准化。
6. **开源核心 + 商业化协作**：客户端与 UE 桥开源吃口碑，团队云库/权限/审批收费。
7. **缩略图 AB 对比**：并排展示两个特效的缩略图，方便选型评审（无需播放，快）。
8. **跨版本兼容提示**：导入时若 UE 版本跨度大，明确提示风险并给出处理建议。
9. **缩略图质量即门面**：由于预览只有一张图，缩略图的取帧/构图直接决定用户是否愿意用——把"选代表性峰值帧"做好，就是低成本高回报。

---

## 7. 开发难点（务必正视）

> 说明：因为预览已简化为「静态缩略图」，原本最重的「引擎内视频渲染」难点大幅降级——难点重心转移到**依赖重映射准确性**、**多版本兼容**与**UE 无头进程编排**。

| # | 难点 | 根因与风险 |
|---|---|---|
| 1 | **依赖解析不准 → 导入变粉**（头号难点） | AssetRegistry 递归依赖可能漏私有/运行时生成资产；材质引用路径跨项目变化，重映射出错就变粉。 |
| 2 | **UE 版本兼容** | 5.0→5.5 的 Niagara/AssetRegistry API 变化大；资产格式跨大版本不保证兼容。桥脚本用 `AssetRegistryHelpers.get_asset_registry()` 等稳定 API，并写入 `engineVersion` 做差量提示。 |
| 3 | **缩略图取帧代表性**（已从"渲染难题"降为"体验难题"） | 粒子随时间变化，取哪一帧最能代表特效需要策略（峰值帧/固定 seek time）；引擎缓存缩略图偶尔是空帧/首帧不好看。相比视频渲染，这是轻量问题。 |
| 4 | **Niagara 内部 API 不稳定** | 很多 Niagara 功能无公开 Python/C++ API，需反射或引入私有头文件，升级易断。优先用公开 Asset Registry / Asset Tools / Editor Asset Library。 |
| 5 | **UE 无头进程编排（新增）** | 拉起 `UnrealEditor.exe` 冷启动慢（10–30s）、`-RenderOffScreen` 在 CI/无 GPU 环境可能失败、进程异常退出需客户端兜底。**缓解**：oneshot + server 双模式、端口/进程健康检查、日志 tail、超时与重试。 |
| 6 | **客户端与引擎结果契约** | request/result JSON 的字段要稳定；大项目导出产物大，需流式写盘 + 进度回调，避免客户端假死。 |
| 7 | **团队版后端成本** | 私有云库、权限、版本、审批需要后端服务，成本和运维陡增（建议放后期）。 |
| 8 | **版权 / License** | 社区互传涉及资产版权，需明确许可协议与侵权处理机制。 |

> 已消除的难点：原「headless 视频渲染慢/吃 GPU/易崩」「整库视频体积膨胀/编码/清理」两条随视频预览取消而不再存在。原「插件崩溃拖垮编辑器」难点也随取消插件而消除——UI 与引擎彻底进程隔离。

---

## 8. 开发建议（落地路线）

### 8.1 技术选型
- **客户端**：**Python 3 + PySide6（Qt）**。跨平台、开发快、复用已写的 UE 桥 Python 脚本；SQLite 做本地索引；`requests`/`http.server` 做 server 模式通信。
- **UE 桥**：纯 **UE5 Python Editor Scripting**，用 `unreal.AssetRegistry` / `unreal.AssetTools` / `unreal.EditorAssetLibrary`。**不需要任何 C++ 插件**，免去编译/多版本编译负担——这是取消插件的最大收益之一。
- **引擎调用**：`UnrealEditor.exe <Project> -ExecutePythonScript=<bridge>/fx_runner.py -unattended -NoSplash -RenderOffScreen`，通过 request/result JSON 通信（见 §3.1）。
- **预览格式**：仅 `JPEG/PNG` 静态缩略图（建议 512×512，单张几十 KB），无视频。
- **缩略图来源**：优先复用引擎缓存缩略图（`GetThumbnailData`）；更好效果用离屏 RenderTarget 截代表性单帧。

### 8.2 MVP 范围（第一版只做这 5 件事，且全部走 UE 桥）
1. **客户端资产浏览器**：选一个 UE 项目 → 拉起 UE 桥 `fx_list.py` → 列出 Niagara/Cascade + 静态缩略图网格。
2. **一键导出 `.fxpack`**：`fx_export.py`（递归依赖 + 生成 manifest + 打包缩略图）。
3. **一键导入 `.fxpack`**：`fx_import.py`（智能重映射 + 冲突处理 + 导入后体检）。
4. **缩略图生成（三档全做）**：Tier1 引擎缓存默认导出；Tier2 峰值帧增强；Tier3 用户手动指定覆盖。组合优先级：用户指定 ＞ 峰值帧 ＞ 引擎缓存。
5. **本地 SQLite 库 + 基础标签/检索**：客户端侧管理已导出/已导入的特效索引，离线可浏览。

> 这 5 件正好覆盖 MVP 卖点「跨项目资产迁移」，且**无 C++ 编译、无插件安装**，风险最低、验证最快。原来插件里的 C++ 缩略图导出代码可整体删除——等效逻辑已在 `fx_thumbnail.py` 里用 Python 实现。

### 8.3 兼容性策略
- 锁定目标版本区间（建议 `5.3+`）。前置检查用客户端正则读 `.uproject` 的 `EngineAssociation` / 注册表定位 UE 路径与版本。
- 资产导出时写入 `engineVersion`，导入端做版本差提示。
- 优先用稳定公开的 **Asset Registry / Asset Tools / Editor Asset Library** API，避开 Niagara 私有内部类。

### 8.4 健壮性
- **客户端侧**：拉起引擎前检测 UE 是否存在/版本达标；操作加超时与重试；tail `ue.log` 实时显示进度；引擎异常退出时给出可读报错而非假死。
- **桥脚本侧**：每个模块包在 `try/except` 里，错误写进 `fx_result.json` 的 `error` 字段，绝不静默失败。
- **依赖提取黑白名单**：黑名单过滤 `/Engine/`、`/Plugin/` 内置资产，避免把引擎文件打进包。
- **导入后强制体检**：自动报告变粉/缺失引用，回传客户端。

### 8.5 增长节奏
- **v0.1–v0.3**：客户端三件套（浏览/导出/导入）+ 引擎缓存缩略图 + 本地库，免费给个人用，攒口碑与资产量。
- **v0.4**：UE 桥 server 常驻模式（避免重复冷启动），体验接近原生。
- **v0.5**：社区市场上线（用户互传），引爆网络效应。
- **v1.0**：代表性峰值帧缩略图（Tier2）+ 性能档位 + 缩略图 AB 对比。
- **v2.0+**：团队私有云库、权限、版本、审批（商业化）。

---

## 9. 风险与边界提醒

- **不要一上来做云和团队功能**：后端成本与合规会拖垮个人版进度。先吃个人用户口碑。
- **预览只做静态缩略图，不要越界做播放**：首版直接复用引擎缓存缩略图导出即可，不要投入 headless 视频渲染那套重工程。
- **客户端必须本机装 UE 才能用全部功能**：纯离线只能看已导出/已缓存的 `.fxpack` 库与缩略图；列资产、出缩略图、导入导出都需临时拉起 UE。这是"无插件纯客户端"架构的固有前提，必须在产品说明里写清楚。
- **UE 无头冷启动慢**：首次操作等 10–30s 是正常的；用 server 常驻模式缓解，不要因此误判为卡死。
- **跨大版本（如 5.0→5.5）资产迁移不保证 100% 成功**：需在 UI 明确标注风险，并提供「导入后体检」兜底。
- **社区资产版权**：上线市场前必须有 License 选择与侵权投诉通道，否则有法律风险。

---

## 10. 一句话总结

> 用「**PySide6 独立客户端做库与界面，UE 无头桥做引擎真相源（缩略图 + 依赖 + 迁移）**」，取消插件后反而更干净：库管理在编辑器外、不污染项目、无 C++ 编译负担。MVP 先啃下**跨项目特效迁移不丢依赖**这块硬骨头，免费个人版积累资产量与口碑，再用社区市场形成网络效应，最后以团队云库商业化。由于预览已简化为**静态缩略图**（直接复用引擎缓存缩略图即可），最重的"引擎内视频渲染"难点被消除，难点集中在「依赖重映射准确性」「多版本兼容」和「UE 无头进程编排」三件事上；用 manifest + deps_graph 把迁移做成确定性可重放的流程，用 request/result JSON 把客户端与引擎解耦。
